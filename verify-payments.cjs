const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');const s=fs.readFileSync('index.html','utf8'),c={};vm.createContext(c);vm.runInContext(s.slice(s.indexOf('        function paymentValidationError('),s.indexOf('        function confirmSale(')),c);const check=c.paymentValidationError;assert.equal(check(100,[{method:'Pix',amount:100}]),'');assert.equal(check(100,[{method:'Pix',amount:40},{method:'Cartão de Crédito',amount:60,installments:3}]),'');for(const p of [[{method:'Pix',amount:-1}],[{method:'Pix',amount:99}],[{method:'Pix',amount:NaN}],[{method:'Cartão de Crédito',amount:100,installments:0}]]) assert(check(100,p));assert(check(-10,[]));assert(check(100,[]));assert.equal(check(100,[],false),'');assert.equal(check(.3,[{method:'Pix',amount:.1},{method:'Dinheiro',amount:.2}]),'');console.log('OK: payment totals, invalid amounts, installments, pending orders and cent rounding.');
