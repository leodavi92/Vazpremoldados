const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');const html=fs.readFileSync('index.html','utf8');let month='2026-09';const nodes={};const ctx={getBreakEvenConfig:()=>({resetDateStr:month+'-01',endDateStr:month+'-30'}),document:{getElementById:id=>nodes[id]||(nodes[id]={value:'',innerHTML:''})},appState:{transactions:[]},lucide:{createIcons(){}},renderFintechRow:t=>JSON.stringify(t)};vm.createContext(ctx);for(const name of ['isInSelectedMonth','filterFullStatement']){const start=html.indexOf('        function '+name+'('),end=html.indexOf('        function ',start+20);vm.runInContext(html.slice(start,end),ctx);}
ctx.appState.transactions=[{id:1,saleId:1,date:'2026-08-31',type:'entrada',amount:100},{id:2,saleId:1,date:'2026-09-01',type:'entrada',amount:200},{id:3,saleId:1,date:'2026-09-30',type:'saida',amount:10,isSaleRefund:true},{id:4,saleId:1,date:'2026-10-01',type:'entrada',amount:300}];const original=JSON.stringify(ctx.appState);ctx.filterFullStatement();let out=nodes['full-statement-list'].innerHTML;assert.ok(out.includes('"id":2'));assert.ok(out.includes('"id":3'));assert.ok(!out.includes('"id":1'));assert.ok(!out.includes('"id":4'));nodes['statement-type'].value='saida';ctx.filterFullStatement();assert.ok(!nodes['full-statement-list'].innerHTML.includes('"id":2'));assert.ok(nodes['full-statement-list'].innerHTML.includes('"id":3'));assert.equal(JSON.stringify(ctx.appState),original);assert.equal(ctx.isInSelectedMonth('2026-09-30'),true);assert.equal(ctx.isInSelectedMonth('2026-08-31'),false);month='2026-10';assert.equal(ctx.isInSelectedMonth('2026-10-01'),true);console.log('OK: shared history month, boundaries, cross-month installments, refund filter and unchanged records.');
