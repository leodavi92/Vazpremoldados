const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');const html=fs.readFileSync('index.html','utf8');const output={innerHTML:''};const c={appState:{config:{showBalance:true},transactions:[],sales:[]},getBreakEvenConfig:()=>({resetDateStr:'2026-09-01'}),document:{getElementById:()=>output},lucide:{createIcons(){}},formatCurrency:n=>n.toFixed(2),renderFintechRow:t=>'<row>'+t.amount+'</row>'};vm.createContext(c);vm.runInContext(html.slice(html.indexOf('        function cashMonthTransactions('),html.indexOf('        function openFullStatementModal(')),c);c.renderCaixa();assert.ok(output.innerHTML.includes('value="2026-09"'));assert.ok(output.innerHTML.includes('Nenhuma movimentação neste mês.'));c.appState.transactions=[{id:1,date:'2026-09-01',amount:100,type:'entrada'},{id:2,date:'2026-09-02',amount:20,type:'saida'},{id:3,date:'2026-08-31',amount:999,type:'entrada'}];c.renderCaixa();assert.ok(output.innerHTML.includes('80.00'));assert.ok(!output.innerHTML.includes('<row>999</row>'));console.log('OK: cash screen opens with empty and populated data, selected month and monthly totals.');
